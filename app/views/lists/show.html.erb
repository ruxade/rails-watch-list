<h1><%= @list.name %></h1>

<p>Bookmarks for this list:</p>
<ul>
  <% @list.bookmarks.each do |bookmark| %>
    <li><%= bookmark.comment %> - Movie: <%= bookmark.movie.title %></li>
  <% end %>
</ul>

<%= link_to 'Back to Lists', lists_path %>
